# work/

Artifacts for **Architecture & Formula Hypothesis Pilot** (public sanitized example).

| Path | Purpose |
|------|---------|
| `hypotheses/` | One file per hypothesis (claim, formula, test plan) |
| `runs/` | Script outputs, numbers, PASS/FAIL notes |
| `arch/` | Architecture sketches (flows, modules, boundaries) |

Start small: one hypothesis file + one run note per milestone.