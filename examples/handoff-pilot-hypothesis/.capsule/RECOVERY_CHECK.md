# Recovery Check

- [ ] AI is on **Architecture & Formula Hypothesis Pilot**, not paused side project or format repo
- [ ] Only **one** item in In Progress; breakthroughs logged in DECISION_LOG
- [ ] `version_hash` consistent across manifest, report, resume
- [ ] `work/` contents match Done section
- [ ] Run: `python scripts/verify_capsule.py --capsule-dir .capsule` (add `--check-git-tag --repo .` if using git tags)

Fix: restore `capsule/<hash>` tag; re-paste RESUME_INSTRUCTIONS.md.